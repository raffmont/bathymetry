# AGENTS

## Scope
These instructions apply to the entire repository.

## Documentation expectations
- Keep documentation paths aligned with the actual repository layout (for example, prefer `data/output` and `data/cache`).
- When referencing example configs, use files that exist under `examples/`.
- Use Markdown links for documents referenced from the README.
